export const piValue = document.querySelector(".pi-value")!;
export const totalValue = document.querySelector(".total-value")!;
export const insideValue = document.querySelector(".inside-value")!;
export const restartBtn = document.querySelector("button.restart")!;
export const stopBtn = document.querySelector("button.stop")!;
export const colors = {
  circle: "#e76f51",
  rect: "#8ecae6",
};
